apiVersion: batch/v1
kind: Job
metadata:
  name: whale-help-output
  namespace: deepseek-code-whale
  labels:
    app.kubernetes.io/name: deepseek-code-whale
    app.kubernetes.io/component: whale
    app.kubernetes.io/managed-by: autopoc
    poc.autopoc/scenario: help-output
spec:
  backoffLimit: 2
  activeDeadlineSeconds: 120
  ttlSecondsAfterFinished: 600
  template:
    metadata:
      labels:
        app.kubernetes.io/name: deepseek-code-whale
        app.kubernetes.io/component: whale
        poc.autopoc/scenario: help-output
    spec:
      volumes:
        - name: whale-home
          emptyDir: {}
      containers:
        - name: whale
          image: quay.io/aicatalyst/deepseek-code-whale-whale:latest
          imagePullPolicy: Always
          command: ["whale"]
          args: ["--help"]
          env:
            - name: HOME
              value: "/tmp/whale-home"
            - name: DEEPSEEK_API_KEY
              valueFrom:
                secretKeyRef:
                  name: whale-secrets
                  key: DEEPSEEK_API_KEY
                  optional: true
          volumeMounts:
            - name: whale-home
              mountPath: /tmp/whale-home
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      restartPolicy: Never
